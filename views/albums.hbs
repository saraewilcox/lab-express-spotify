<div>
    {{#each resultAlbums}}
    <div>
        <h3>{{this.name}}</h3>
        <img src={{this.images.[0].url}} alt="album-pic">
        <a href="/tracks/{{this.id}}">View Tracks</a>
    </div>
    {{/each}}
</div>